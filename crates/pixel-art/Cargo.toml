[package]
name = "pixel-art"
version = "0.1.0"
edition = "2024"

[dependencies]
image = { version = "0.25.10", optional = true }
tiny-skia = { version = "0.12.0", optional = true }

[features]
image = ["dep:image"]
tiny-skia = ["dep:tiny-skia"]
